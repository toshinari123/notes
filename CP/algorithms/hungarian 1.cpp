#include <cstdio>
//made by toshi 
int main(){
	int n;										//size of a half, so graph has 2n nodes
	scanf("%d", &n);
	int w[n][n];								//the weights from ith node in X to jth node in Y
	for (int i = 0; i < n; i++){
		for (int j = 0; j < n; j++){
			scanf("%d", &w[i][j]);
		}
	}
	int lx[n], ly[n];							//labeling of X and Y
	for (int i = 0; i < n; i++){
		for (int j = 0; j < n; j++){
			
		}
	}
}
